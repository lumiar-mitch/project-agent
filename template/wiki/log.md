# Wiki log

Append-only, newest at the bottom. The agent records every ingest, query worth keeping, review and
lint pass here — what was read, which items/pages were touched, decisions made. One entry per
operation.

Format:

```
## [YYYY-MM-DD] ingest|capture|query|review|lint | Short title
- 1–5 bullet lines
```

<!-- entries below -->
